# Environment Variables

- Use the `.env` file at the project root for both frontend and backend configuration.
- Example variables:
  - `API_BASE_URL` for backend
  - `VITE_API_BASE_URL` for frontend (Vite)
- Remove `.env` and `.env.example` from frontend and backend folders to avoid confusion.
